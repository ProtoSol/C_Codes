#include <stdio.h>

int main(){
    //For Numbers, whole numbers to be specific
    int Age = 18;
    //Stores single character only but after using [] we can store multiple characters but cannpt be chaanged later on.
    char Name[] = "Pranshu";
    //Stores decimal number and float also does that.
    double CGPA = 8.5;
    //Simple character datatype storing single character  with single quote.
    char True = 'Y',
    // These are The Basic Characters That Are used.
}